#!/usr/bin/env python
from setuptools import setup, find_packages
 
setup(
    name="patch-notify",
    author="Matthew Palmer",
    description="Python Executable which performs Email Notifications From Foreman",
    packages=find_packages()
)